## Frontend (protótipo)

Este diretório contém uma implementação em HTML/CSS da Home baseada no layout que você enviou.

### Abrir

Você pode abrir o arquivo `frontend/index.html` direto no navegador.

Se preferir servir localmente:

```powershell
npx serve frontend
```

